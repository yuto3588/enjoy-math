# 設計・運用のメモは公開リポジトリに入れない。
# GitHub Pages を無料で使うにはリポジトリが公開になるため、
# 個人にまつわる記述を含むファイルはここで外している。
CLAUDE.md
DESIGN.md

# 学習の記録。子ども本人の記録なので、絶対に公開しない。
# サーバーが列を足すときに作る .bak も同じ中身なので、まとめて外す。
study-log*.csv
study-log*.csv.bak

# この PC で動かすためだけのもの（アプリ本体には関係ない）
start-server.bat

# Python が勝手に作るもの
__pycache__/
*.pyc

# OS / OneDrive が勝手に作るもの
Thumbs.db
desktop.ini
.DS_Store
